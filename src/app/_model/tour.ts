import { Url } from 'url';

export class Tour {
            picture: Url;
            audio: Url;
            lat: number;
            long:number;
            feedback:string;
            _id: string;
            name: string;
            description:string;
            categoryName: string;
            transcript: string;
}

